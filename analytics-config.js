/* AEM GROUP analytics configuration — no visual changes.
   Add the real Google Analytics 4 Measurement ID (G-XXXXXXXXXX) when available.
   Leave empty to keep analytics disabled until configured. */
window.AEM_ANALYTICS = { ga4: "" };
